import { PageSkeleton } from '../components/ui/Skeleton'

// Owned by Member 2 (Catalog & Discovery) — see TEAM.md
export function ProductDetailsPage() {
  return <PageSkeleton title="Product Details" />
}
