import { PageSkeleton } from '../components/ui/Skeleton'

// Owned by Member 3 (Transactions & Saga) — saga-state timeline, see ARCHITECTURE.md §6
export function OrderTrackingPage() {
  return <PageSkeleton title="Order Tracking" />
}
